import { createTheme } from "@mui/material/styles"

export const theme = createTheme({
    palette: {
    type: 'light',
    primary: {
      main: '#01579b',
    },
    secondary: {
      main: '#f5b300',
    },
    background: {
      default: '#ffa726',
      paper: '#b3e5fc',
    },
  },})
